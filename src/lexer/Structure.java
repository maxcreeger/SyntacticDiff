package lexer;

import tokenizer.Token;

/**
 * A structured set of {@link Token}s
 *
 * @param <G> a grammar to make sense of the structure
 */
public interface Structure<G extends Grammar> {
    // Anything to do ?
}
